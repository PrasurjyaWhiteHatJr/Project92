import React from react-native
import {StyleSheet, Text, Image, View} from react-native

export default class extends RandomScreen from React.Component(
    render(){
        return(
            <View>
        )
    }
)